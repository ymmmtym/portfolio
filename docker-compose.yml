version: '3'
services:
    portfolio:
        build: .
        container_name: portfolio
        hostname: portfolio
        tty: true
        ports:
            - "8080:8080"
            - "9005:9005"
        volumes:
            - ./app:/app
